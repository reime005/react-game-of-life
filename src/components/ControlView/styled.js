import styled, { css } from 'styled-components';

const StyledControlView = styled.section`
  width: 30%;
  height: 100%;
  background-color: #cccccb;
  box-sizing: border-box;
  position: fixed;
  transform: translateZ(0);
  display: block;
  z-index: 1;
  left: 0;
  top: 0;
  bottom: 0;
  right: auto;
  color: white;
  overflow-y: scroll;
  padding: 25px;
`;

export default StyledControlView;
